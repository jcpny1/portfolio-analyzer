import React from 'react';

const HelpPage = (props) => {
  return (
    <ul>
      <li>The trade prices shown are not necessarily the latest ones. This is a delayed feed with one minute resolution.</li>
    </ul>
  );
}

export default HelpPage;
